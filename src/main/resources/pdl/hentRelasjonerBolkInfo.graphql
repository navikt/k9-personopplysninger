query($identer: [ID!]!) {
    hentPersonBolk(identer: $identer) {
        ident,
        person {
            familierelasjoner {
                relatertPersonsIdent
                relatertPersonsRolle
                minRolleForPerson
            }
        },
        code
    }
}